{
  "name": "glossy-monorepo",
  "private": true,
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "scripts": {
    "dev:front": "npm.cmd --prefix glossy-landing run dev",
    "dev:server": "npm.cmd --prefix server run dev",
    "dev:all": "concurrently \"npm.cmd --prefix glossy-landing run dev\" \"npm.cmd --prefix server run dev\""
  }
}
